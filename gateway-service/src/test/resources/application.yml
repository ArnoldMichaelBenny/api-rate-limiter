# This is the property that fixes the build failure
spring:
  application:
    name: gateway-service-test

# Your well-structured test properties
monitoring-service:
  url: "http://localhost:9999/test-monitoring"

rate-limiter:
  default-limit: 10
  configs: [] # Empty list is perfect for the context test